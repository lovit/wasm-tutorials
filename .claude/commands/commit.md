---
description: 변경사항을 의미 단위로 나눠 한국어 conventional commit 을 만든다
allowed-tools:
  - Bash
---

@.claude/rules/git-workflow.md 의 **Commit 실행 절차** 를 따라 커밋하라.

커밋 본문에도 하드랩을 넣지 않는다. 문단은 한 줄로 쓴다.

`Co-Authored-By` 트레일러는 붙이지 않는다.
